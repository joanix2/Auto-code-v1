# ============================================
# AutoCode Platform - Environment Variables
# ============================================

# ----------------
# Neo4j Database
# ----------------
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=change_me_in_production

# ----------------
# GitHub OAuth (for user authentication)
# ----------------
# Create OAuth App: https://github.com/settings/developers
# Homepage URL: http://localhost:3000 (or your domain)
# Callback URL: http://localhost:8000/api/auth/github/callback (or your domain)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_REDIRECT_URI=http://localhost:8000/api/auth/github/callback

# ----------------
# JWT Authentication
# ----------------
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_run_openssl_rand_hex_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ----------------
# Application URLs
# ----------------
# Frontend URL (where the React app is served)
FRONTEND_URL=http://localhost:3000

# Backend URL (internal Docker network)
BACKEND_URL=http://backend:8000

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# ----------------
# Environment
# ----------------
ENVIRONMENT=development

# ============================================
# AWS Deployment Variables (for Terraform)
# ============================================
# These are used when deploying to AWS with Terraform
# Set these when running: terraform apply

# AWS Region
TF_VAR_aws_region=eu-west-3

# Project name (used for resource naming)
TF_VAR_project_name=autocode

# Public IP of your EC2 instance (auto-populated by Terraform output)
# After terraform apply, copy the public_ip output here
# EC2_PUBLIC_IP=35.181.22.28

# ============================================
# GitHub Actions Secrets (auto-configured by Terraform)
# ============================================
# These secrets are automatically created in your GitHub repository
# when you run: terraform apply
# 
# Required Terraform variables:
# - TF_VAR_github_token (GitHub PAT with repo and admin:repo_hook scopes)
# - TF_VAR_github_owner (your GitHub username)
# - TF_VAR_github_repo (repository name, e.g., Auto-code-v1)
#
# Auto-configured secrets:
# - SSH_PRIVATE_KEY (from ~/.ssh/aws_key.pem)
# - EC2_PUBLIC_IP (from Terraform output)
# - NEO4J_PASSWORD (from this .env file)

