# Documentation technique

## Configuration des agents OpenCode

Les permissions déclarées dans le frontmatter des fichiers
`.opencode/agents/*.md` utilisent des règles explicites (`allow`, `ask` ou
`deny`). Les booléens `true` et `false` ne sont pas acceptés par le schéma de
configuration OpenCode actuel.
