# OpenCode Service - Environment Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token (required)
# Generate at: https://github.com/settings/tokens
GH_TOKEN=

# Optional: Custom SSH key path (default: ~/.ssh/id_ed25519)
# OPENCODE_SSH_KEY=~/.ssh/id_ed25519

# Optional: OpenCode auth file path (default: ~/.local/share/opencode/auth.json)
# OPENCODE_AUTH_FILE=~/.local/share/opencode/auth.json

# Optional: OpenCode config directory (default: ~/.config/opencode)
# OPENCODE_CONFIG_DIR=~/.config/opencode

# Docker container name (default: autocode-opencode)
# CONTAINER_NAME=autocode-opencode

# Docker image name (default: autocode-opencode-service)
# IMAGE_NAME=autocode-opencode-service
